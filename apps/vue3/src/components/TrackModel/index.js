import ModelConstructor from './ModelWrapper.vue';

/**
 * 创建跟随弹窗
 * @param content
 * @param {import('track').CreateOptions} options
 * @param modelProps
 */
export function createTrackModel(content, options, modelProps) {}
